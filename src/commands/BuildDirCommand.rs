use crate::command_traits::Command;

mod build_dir {
    struct BuildDirCommand {}

    impl Command for BuildDirCommand {
        fn do_the_thing(args: &[String]) {

        }
    }
    //parse Args

    //get
}
